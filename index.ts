// let nome: string = "Daniel";
// console.log("Olá, " + nome);
const ask = require('readline-sync');

function vezes(a: number): number {
    return a * 2
}

let nome: number = ask.question('')